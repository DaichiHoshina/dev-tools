# VITE_GITLAB_TOKEN はローカル開発でのみ使う。VITE_ prefix の値はビルド時に
# JS へ展開されるため、配信するビルドではこの変数を設定しない。利用者は
# ヘッダーの鍵ボタンからトークンを入力する (保存先は各自のブラウザ)。
VITE_GITLAB_TOKEN=your-gitlab-personal-access-token
VITE_GITLAB_URL=https://gitlab.example.com
VITE_BASE_PATH=/devtools/gitlab-grep/
